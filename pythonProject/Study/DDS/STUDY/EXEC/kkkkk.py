from random import randint
import numpy as np

"""lista = []
for i in range(100):
    lista.append(random.randint(0, 500))

arreglo = np.array(lista)
print(arreglo)"""

arreglo = np.array([[], []])
print(arreglo.shape)
